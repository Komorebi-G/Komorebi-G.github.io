{
  "name": "lbh-algorithm-lab",
  "type": "module",
  "version": "0.1.0",
  "scripts": {
    "sync:demos": "node scripts/sync-demos.mjs",
    "predev": "npm run sync:demos",
    "dev": "astro dev",
    "prebuild": "npm run sync:demos",
    "build": "astro check && astro build",
    "build:pages": "astro check && astro build",
    "preview": "astro preview"
  },
  "dependencies": {
    "@astrojs/check": "^0.9.4",
    "astro": "^5.7.0",
    "typescript": "^5.7.0"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.1.0",
    "tailwindcss": "^4.1.0"
  }
}
